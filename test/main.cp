// #include <iconv.h>
// #include <stdarg.h>

// int args(const char *c, ...) {
//     va_list v;
//     va_start(v, c);
//     va_arg(v, int);
//     va_end(v);
//     return 34;
// }

// int printf(const char *c, ...);

// typedef struct foo *bar;
// bar dobar() {
// }

int main() {
    int *bar[2];
    // fpos_t f;
    // iconv_t cd;
    // cd = iconv_open ("IBM1047", "UTF-8");
    // if (cd == (iconv_t) -1)
    //     return 1;
    return 0;
}